const agreementSchema = require("../schemas/agreement.schema.json");
const deliverableSchema = require("../schemas/deliverable.schema.json");
const disputeSchema = require("../schemas/dispute.schema.json");

module.exports = {
  agreementSchema,
  deliverableSchema,
  disputeSchema,
};
